{{ define "content" }}
<form method="POST" action="/post/register/do">
    <div class="form-group">
        <label>タイトル</label><br>
        <input class="form-control" type="text" name="title" value="{{.Title}}" required />
    </div>
    <div class="form-group">
        <label>URL</label><br>
        <span><a href="{{.Url}}">{{.Url}}</a></span>
        <input class="form-control" type="hidden" name="url" value="{{.Url}}" required />
    </div>
    <div class="form-group">
        <label>イメージ画像</label><br>
        <input class="form-control" type="hidden" name="image" value="{{.Image}}" required />
        <div>
            <img src="{{.Image}}" width="200px" height="200px">
        </div>
        <span><a href="{{.Image}}">{{.Image}}</a></span>
    </div>
    <div class="form-group">
        <div class="dropdown dropright">
            <label>記事の目的</label>
            <select name="usecase">
                <option value="おもしろい">おもしろい</option>
                <option value="エラー解決">エラー解決</option>
                <option value="知識">知識</option>
                <option value="リファレンス">リファレンス</option>
                <option value="チュートリアル">チュートリアル</option>
                <option value="その他">その他</option>
            </select>
        </div>
    </div>
    <div class="form-group">
        <label>ジャンル</label>
        <select name="genre">
            <option value="プログラミング言語">プログラミング言語</option>
            <option value="フレームワーク">フレームワーク</option>
            <option value="ライブラリ">ライブラリ</option>
            <option value="OS">OS</option>
            <option value="SQL">SQL</option>
            <option value="クラウド">クラウド</option>
            <option value="コンテナ技術">コンテナ技術</option>
            <option value="デザイン">デザイン</option>
            <option value="その他">その他</option>
        </select>
    </div>
    <div class="form-group">
        <label>記事の要約</label><br>
        <textarea class="form-control" type="text" maxlength="250" name="description" placeholder="250文字以内でクリップしたい記事の要約を書いてください" value="{{.Description}}" required></textarea>
    </div>
    <div>
        <input class="clipButton" type="submit" value="投稿する">
    </div>
</form>
{{ end }}